%body
  .title= link_to "Sign in with Facebook", "/auth/facebook"
  .title= link_to "Sign in with Twitter", "/auth/twitter"
-#  if current_user
-#    Welcome = current_user.name
-#  else
-#    = link_to "Sign in with Facebook", "/auth/facebook"
-#  end